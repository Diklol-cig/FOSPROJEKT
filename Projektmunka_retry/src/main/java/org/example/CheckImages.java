package org.example;

import java.io.File;
import java.util.List;

public class CheckImages {
/*
    public static boolean hasPrev(List<File> paths){
        for(File p : paths) {
            boolean hasPrev = paths.indexOf(p) != 0 && paths.get(paths.indexOf(p) - 1).getParent().equals(p.getParent());
            boolean hasNext = paths.indexOf(p) != (paths.size() - 1) && paths.get(paths.indexOf(p) + 1).getParent().equals(p.getParent());
        }
    }

    public static boolean hasNext(List<File> paths){
        for(File p : paths){
            //boolean hasPrev = paths.indexOf(p) != 0 && paths.get(paths.indexOf(p) - 1).getParent().equals(p.getParent());
            boolean hasNext = paths.indexOf(p) != (paths.size() -1) && paths.get(paths.indexOf(p) + 1).getParent().equals(p.getParent());
        }
    }
*/
}
